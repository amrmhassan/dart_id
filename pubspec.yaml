name: dart_id
description: Easy way of creating ids in dart.
version: 2.0.0
repository: https://github.com/amrmhassan/dart_id

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
